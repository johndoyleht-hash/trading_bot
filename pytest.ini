[pytest]
addopts = -q
testpaths = tests

markers =
    smoke: quick sanity tests
    regression: locked backtest baselines
    forward: forward-config health checks
    portfolio: portfolio forward-health checks (PF/DD/FinalEq)
