# pytest.ini
[pytest]
addopts = -q
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    regression: end-to-end baseline regression tests
    forward: looser out-of-sample health checks
